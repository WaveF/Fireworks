var sel = fw.selection;
var newSel = [];
var copies = prompt("请输入复制次数：", "10");

newSel.push(sel[0]);

for(var i=0; i<copies; i++){
	fw.getDocumentDOM().cloneSelection();
	newSel.push(fw.selection[0]);
}

fw.selection = newSel;